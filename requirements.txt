# Agente local — dependencias de runtime (deliberadamente mínimas: la app pesa MB, no GB).
PyYAML>=6.0
requests>=2.31
# Canal de control con la web de claude-test (WebSocket saliente).
websocket-client>=1.7
